package org.example.click_up.repository;

import org.example.click_up.model.Task_history;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Task_historyRepository extends JpaRepository<Task_history, Long> {
}
