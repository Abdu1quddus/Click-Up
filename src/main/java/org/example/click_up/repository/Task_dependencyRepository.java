package org.example.click_up.repository;

import org.example.click_up.model.Task_dependency;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Task_dependencyRepository extends JpaRepository<Task_dependency, Long> {
}
